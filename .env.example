# Copy to ~/.third-brain/.env and fill in values

# Neo4j — only needed if you want the knowledge graph layer
# Leave blank to run without graph (vector + BM25 + reranker still work)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=
